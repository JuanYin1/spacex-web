Recap
1. background-image: apply pic or gif a background
2. font search from google: copy link to the head and body
3. difference between <span></span>(inline) and <div></div>(block)
4. difference between id(only use once) and clas(use multiple times)
5. text-shadow:create 3D-ish text